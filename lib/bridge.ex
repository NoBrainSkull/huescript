defmodule Huescript.Bridge do
  # List all detected Hue Bridges
  def discover do
    :not_impl
  end

  # Connect to a Hue Bridge
  def connect do
    :not_impl
  end

  # Get rid of current connexion
  def disconnect do
    :not_impl
  end
end
